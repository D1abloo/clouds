{
  "name": "@cloudops/workers",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn src/main.ts",
    "lint": "eslint src --ext .ts",
    "test": "jest"
  }
}
